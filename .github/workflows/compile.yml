name: Build and Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          cmake \
          g++ \
          libnetcdf-dev \
          libnetcdf-c++4-dev \
          libyaml-cpp-dev \
          libboost-all-dev \
          libfmt-dev \
          libeigen3-dev
    
    - name: Create build directory
      run: mkdir -p build
    
    - name: Configure with CMake
      run: |
        cd Code.v05-00
        rm -rf build
        mkdir -p build
        cd build
        cmake .. \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_TOOLCHAIN_FILE= \
          -DBUILD_TESTING=OFF
    
    - name: Build Core and KPP libraries only
      run: |
        cd Code.v05-00/build
        # Build only Core and KPP - do NOT use default 'all' target
        cmake --build . --target Core
        cmake --build . --target KPP
        echo "Build completed without EPM!"